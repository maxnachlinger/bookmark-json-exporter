const tree = 'tree'
const flattened = 'flattened'
export const exportTypes = {tree, flattened}

const exportRequested = 'exportRequested'
const exportFailed = 'exportFailed'
const exportOk = 'exportOk'
export const messageTypes = {exportRequested, exportFailed, exportOk}
